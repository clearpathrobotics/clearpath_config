# Samples

## Platform Samples

Each platform has a set of sample YAML files that are used for testing and should be used as the base file to create custom robot configurations.

## Test Sample Coverage

The test samples cover the entire inventory of attachments, sensors, mounts, and manipulators.
